# aiport-verify
Independent verdict re-verifier for the Agent Commerce Protocol (ERC-8183). Recompute an ACP job's verdict from finalized chain state (tx success, finality depth, reorg check) and optionally anchor it to EAS.
